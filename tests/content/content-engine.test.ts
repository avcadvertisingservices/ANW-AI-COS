import { describe,expect,it } from "vitest";import { createKnowledgeEntry,InMemoryKnowledgeRepository,KnowledgeService } from "../../src/modules/knowledge/index.js";import { ContentEngineService,ContentKnowledgeRetriever,validateRequest } from "../../src/modules/content/engine.js";import { MockContentProvider } from "../../src/modules/content/providers.js";
describe("AI Content Engine",()=>{it("validates slide counts",()=>{expect(()=>validateRequest({topic:"hearing loss",audience:"patients",formats:["carousel"],tone:"educational",language:"English",carouselSlideCount:3,knowledgeLimit:5})).toThrow()});it("creates a complete 10-slide bundle",async()=>{const repo=new InMemoryKnowledgeRepository();const ks=new KnowledgeService(repo);await ks.create(createKnowledgeEntry({id:"knowledge.hearing.approved",slug:"hearing-approved",title:"One-Sided Hearing Loss",summary:"Approved education about one-sided hearing loss.",body:"This approved entry explains the topic without diagnosing an individual.",category:"symptom",status:"approved",medicalReviewRequired:true,reviewedBy:"Medical Reviewer",reviewedAt:new Date().toISOString(),tags:["hearing"],keywords:["one-sided hearing loss"]}));const engine=new ContentEngineService(new ContentKnowledgeRetriever(ks),new MockContentProvider());const b=await engine.generate({topic:"one-sided hearing loss",audience:"Acoustic Neuroma patients",formats:["blog","facebook","carousel","reel","pinterest","email","youtube"],tone:"compassionate",language:"English",carouselSlideCount:10,knowledgeLimit:5});expect(b.carousel?.slides).toHaveLength(10);expect(b.blog).not.toBeNull();expect(b.brand.includesMissionMessage).toBe(true);});});
