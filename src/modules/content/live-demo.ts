import "dotenv/config";import { promises as fs } from "node:fs";import path from "node:path";import { createOpenAIContentEngine } from "./factory.js";
const main=async()=>{const b=await createOpenAIContentEngine().generate({topic:"one-sided hearing loss",audience:"Acoustic Neuroma patients, survivors, and caregivers",formats:["blog","facebook","carousel","reel","pinterest","email","youtube"],tone:"compassionate",language:"English",carouselSlideCount:10,knowledgeLimit:5});const dir=path.resolve("output");await fs.mkdir(dir,{recursive:true});const file=path.join(dir,`content-bundle-${Date.now()}.json`);await fs.writeFile(file,JSON.stringify(b,null,2),"utf8");console.log({bundleId:b.id,model:b.model,status:b.status,carouselSlides:b.carousel?.slides.length??0,safetyWarnings:b.safety.warnings,brandWarnings:b.brand.warnings,outputPath:file});};main().catch(e=>{console.error(e);process.exitCode=1});
